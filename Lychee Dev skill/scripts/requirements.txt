# Optional dependencies for the live-window automation steps
# (capture/send/run/bugs). Verified on Windows x64 with Python 3.12:
#
#   python -m pip install --target <dir> -r requirements.txt
#
# zxing-cpp 3.x is required: only read_barcode/read_barcodes exist, and the
# tooling calls read_barcodes. windows-capture 2.x exposes window_hwnd,
# on_frame_arrived/on_closed handlers, Frame.convert_to_bgr/crop and
# start_free_threaded. opencv is a transitive requirement of windows-capture.
zxing-cpp==3.1.1
windows-capture==2.0.1
numpy==2.5.3
opencv-python==5.0.0.93
